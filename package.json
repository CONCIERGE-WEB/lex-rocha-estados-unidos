{
  "name": "judicial-intelligence",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo — Next.js app in site/ (Vercel Root Directory must be: site)",
  "scripts": {
    "build": "npm run build --prefix site",
    "dev": "npm run dev --prefix site",
    "vercel-build": "npm install --prefix site && npm run build --prefix site"
  },
  "dependencies": {
    "next": "14.2.35",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "engines": {
    "node": ">=18"
  }
}
